# R010 — Completion Report for H014

**Role:** Implementor
**Handoff:** H014
**Date:** 2026-07-08
**Status:** approved

> **Orchestrator review (2026-07-09):** Accepted under D024. Independently reran
> `pnpm typecheck` and `pnpm test`; both pass with 37/37 tests green. The app/package
> `20 rounds|20-round|Round .*20` search only finds the intentional
> `10–20 rounds` mode-select copy.

## Summary

Implemented D025 variable Classic Run lengths in runtime code. `CLASSIC_RUN_ROUNDS` is now a difficulty-keyed map (Easy 10 / Medium 15 / Hard 20); `createRunState` defaults `totalRounds` from that map while preserving explicit overrides. Landing, mode-select, and Classic setup copy no longer claim every Classic Run is 20 rounds; setup cards show the per-difficulty length.

## Acceptance Criteria Self-Check

| # | Criterion (short) | Result | How verified |
|---|-------------------|--------|--------------|
| 1 | Easy default totalRounds === 10 | ✅ | `pnpm test` — `creates an easy run with 12500 bankroll` asserts `totalRounds` 10 |
| 2 | Medium default totalRounds === 15 | ✅ | `pnpm test` — medium defaults expect `totalRounds` 15 |
| 3 | Hard default totalRounds === 20 | ✅ | `pnpm test` — hard defaults expect `totalRounds` 20 |
| 4 | Explicit totalRounds override still works | ✅ | Existing tests still pass with `totalRounds: 1/3/5` overrides |
| 5 | Classic setup shows correct rounds per difficulty | ✅ | `apps/web/app/play/classic/page.tsx` renders `{CLASSIC_RUN_ROUNDS[d.key]} rounds` |
| 6 | App copy no longer says all Classic Runs are 20 rounds | ✅ | Grep on `apps`/`packages`; only remaining hit is intentional `10–20 rounds` |
| 7 | `pnpm typecheck` passes | ✅ | `pnpm typecheck` — both packages Done |
| 8 | `pnpm test` passes | ✅ | `pnpm test` — 37 passed / 0 failed |

## Files Changed

- `packages/game-engine/src/confidence.ts` — `CLASSIC_RUN_ROUNDS` → `Record<Difficulty, number>`
- `packages/game-engine/src/run.ts` — default `totalRounds` from difficulty map
- `packages/game-engine/tests/run.test.ts` — default totalRounds assertions (10/15/20)
- `apps/web/app/page.tsx` — remove hardcoded “20 rounds.”
- `apps/web/app/play/page.tsx` — “10–20 rounds …”
- `apps/web/app/play/classic/page.tsx` — import `CLASSIC_RUN_ROUNDS`; dynamic per-difficulty count
- `agents/handoffs/H014_variable_classic_run_lengths.md` — status → complete
- `progress.md` — session log + current status
- `agents/reports/R010_H014.md` — this report

## Tests

```text
pnpm typecheck  — pass (game-engine + apps/web)
pnpm test       — 37 passed / 0 failed (3 files)
```

`rg` is not on PATH in this Windows shell; equivalent search via workspace grep:

```text
pattern: 20 rounds|20-round|Round .*20
apps:     1 hit — apps/web/app/play/page.tsx “10–20 rounds” (intentional D025 copy)
packages: 0 hits
```

Hard difficulty still correctly displays “20 rounds” via `{CLASSIC_RUN_ROUNDS[d.key]}` (not a flat global claim).

## Deviations from the Handoff

None.

## Known Issues / Follow-ups

None for H014. Prototype content guessability issues remain accepted under D024 / Phase 3.

## Blocked / Questions for the Orchestrator

None.

## Recommended Next Step

Orchestrator review of this report + uncommitted diff under D024; then commit if approved. Parallel track: dispatch H013 (C002 scenario information design) before Phase 3 schema work.
