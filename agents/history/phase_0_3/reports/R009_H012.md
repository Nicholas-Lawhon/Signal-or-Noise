# R009 — Completion Report for H012

**Role:** Implementor
**Handoff:** H012
**Date:** 2026-07-09
**Status:** approved (accepted under D024; committed by orchestrator)

## Summary

Completed Phase 2 game-engine hardening in `packages/game-engine` only. Added streak tracking on `RunState` / `RunSummary`, exported `advanceRun` (wraps `applyRoundResult`), and `calculateLeaderboardTiebreakers` with the roadmap sort key (bankroll → Signal Score → correct calls → fewer passes → faster completion). Input guards cover negative starting bankroll, totalRounds < 1, and invalid leaderboard inputs. Locked scoring math was not changed.

## Acceptance Criteria Self-Check

| # | Criterion (short) | Result | How verified |
|---|-------------------|--------|--------------|
| 1 | Phase 2 API exports | ✅ | All eight functions exported from package index; types include `ApplyRoundResultInput`, `AdvanceRunOutput`, leaderboard types |
| 2 | Existing scoring unchanged + new tests | ✅ | Prior 24 cases still present; suite now 37 passing |
| 3 | Streak rules (correct / wrong / Pass) | ✅ | `tests/run.test.ts` streak block |
| 4 | `advanceRun` returns run, round, summary only when ended | ✅ | Ongoing / completed / bankrupt cases in `tests/run.test.ts` |
| 5 | Leaderboard `sortKey` order | ✅ | `tests/leaderboard.test.ts` asserts all five positions + named fields |
| 6 | Guard tests for createRunState + tiebreakers | ✅ | Negative bankroll, totalRounds < 1, non-finite / negative leaderboard fields |
| 7 | `pnpm typecheck` and `pnpm test` pass | ✅ | Both green from repo root (PATH prefix as handoff) |
| 8 | `progress.md` + `R009_H012.md`; uncommitted | ✅ | This report + progress session log; no commit/push |

## Files Changed

- `packages/game-engine/src/types.ts` — streak fields; `ApplyRoundResultInput`, `AdvanceRunOutput`, leaderboard types
- `packages/game-engine/src/run.ts` — streak logic, guards, `advanceRun`, summary streak fields
- `packages/game-engine/src/leaderboard.ts` — **new** `calculateLeaderboardTiebreakers`
- `packages/game-engine/src/index.ts` — re-export leaderboard
- `packages/game-engine/tests/run.test.ts` — streak, advanceRun, createRunState guards
- `packages/game-engine/tests/scoring.test.ts` — exact 0 return incorrect for long/short
- `packages/game-engine/tests/leaderboard.test.ts` — **new**
- `agents/handoffs/H012_phase2_game_engine_hardening.md` — status → complete
- `progress.md` — Current Status + session log
- `agents/reports/R009_H012.md` — this report

## Tests

```powershell
$env:PATH = "$env:LOCALAPPDATA\nodejs\node-v24.18.0-win-x64;$env:PATH"
pnpm typecheck   # pass (game-engine + apps/web)
pnpm test        # 37 passed / 0 failing (3 files)
```

## Deviations from the Handoff

None material. For short + `actualReturnPercent: 0`, IEEE float can yield `pnlAmount === -0`; the new zero-return test uses `toBeCloseTo(0)` for short PnL without changing scoring code.

## Known Issues / Follow-ups

- Web UI does not surface streaks yet (out of scope; `createRunState` already initializes fields so typecheck stays green).
- Phase 2 roadmap “full required test matrix (10 cases)” from doc 10 was already largely covered in H001; this pass expands beyond that with streak/advance/leaderboard/zero-return/guards.

## Blocked / Questions for the Orchestrator

None.

## Recommended Next Step

Orchestrator review of this report + uncommitted diff under D024; commit if accepted. Then Phase 3 content pipeline or Gate A growth track as planned.
