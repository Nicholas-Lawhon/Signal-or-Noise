# R019 - Completion Report for H021

**Role:** Implementor  
**Handoff:** H021  
**Date:** 2026-07-09  
**Status:** rejected

## Summary

Implemented the offline Gate 2 harness for `packages/content`: pinned config
(`grok-4.5` / `guess.v1+direction.v1`), optional `review.gate2` schema/types,
pure payload render+hash, pure threshold evaluation, validation integration for
*stored* results only, and a Node-only `gate2 export|check` CLI. Default
validate/test/typecheck stay fully offline and green. Blind payloads exported to
`agents/gate2/H022_payloads.json` (18 entries = 6 active × 3 difficulties). No
model API, no keys, no active-seed `review.gate2` population.

## Acceptance Criteria Self-Check

| # | Criterion (short) | Result | How verified |
|---|-------------------|--------|--------------|
| 1 | `review.gate2` schema/types optional + valid shape | pass | Zod schema + types; validation tests accept valid stored result |
| 2 | Payload exclude answer/reveal; hash behavior | pass | `gate2-payload.test.ts` (9 tests) |
| 3 | Offline evaluator D031 thresholds + warnings | pass | `gate2-evaluate.test.ts` (11 tests) |
| 4 | `gate2 export` blind payloads for active variants | pass | export → 18 payloads at `agents/gate2/H022_payloads.json` |
| 5 | `gate2 check` offline; missing = info only | pass | check → 0 errors, 18 missing INFO, exit 0 |
| 6 | content `validate` offline on active seeds | pass | 6/6 pass, 0 warnings |
| 7 | content `test` with new Gate 2 tests | pass | 46 content tests |
| 8 | root `pnpm test` | pass | 83 total (37 engine + 46 content) |
| 9 | root `pnpm typecheck` | pass | content + game-engine + web |
| 10 | no API keys/client; Node-only imports justified | pass | hits only `gate2/run.ts` `node:fs|path|url` (CLI) |

## Files Changed

**Created**
- `packages/content/src/gate2/config.ts`
- `packages/content/src/gate2/payload.ts`
- `packages/content/src/gate2/evaluate.ts`
- `packages/content/src/gate2/run.ts`
- `packages/content/src/runGate2.ts`
- `packages/content/tests/gate2-payload.test.ts`
- `packages/content/tests/gate2-evaluate.test.ts`
- `agents/gate2/H022_payloads.json` (generated; leave uncommitted unless desired)
- `agents/reports/R019_H021.md`

**Modified**
- `packages/content/package.json` — `gate2` script
- `packages/content/src/schema.ts` — optional `review.gate2`
- `packages/content/src/types.ts` — Gate 2 types
- `packages/content/src/validation.ts` — `checkGate2StoredResults`
- `packages/content/tests/validation.test.ts` — H021 Gate 2 cases
- `agents/handoffs/H021_gate2_grok_validator.md` — status → complete
- `progress.md` — session entry + current status

## Tests

```text
pnpm --filter @signal-or-noise/content test  → 46 passed
pnpm test                                    → 83 passed (37 + 46)
pnpm typecheck                               → pass
pnpm --filter @signal-or-noise/content validate → 6/6
pnpm --filter @signal-or-noise/content gate2 -- export --out agents/gate2/H022_payloads.json → 18 entries
pnpm --filter @signal-or-noise/content gate2 -- check → 0 errors, 18 missing info
```

## Deviations from the Handoff

None material. Plausible-count WARN checks follow C003 Q2 detail (floor + min
counts) under the handoff’s “plausible floor WARN-only” line. Export path
resolution treats relative `--out` as monorepo-root relative so
`agents/gate2/...` works from the content package CLI.

## Known Issues / Follow-ups

- Active scenario **ids** embed company slugs (`scenario_amazon_…`). Export
  includes `scenarioId` as required for write-back; payload *fields* are clean.
  H022 judge presentation should avoid showing raw ids to the model if full
  blindness is required.
- Missing `review.gate2` still does **not** fail validation (intentional H021).
  Enforce after blind results land.
- `node:fs|path|url` appear only in `packages/content/src/gate2/run.ts` (CLI).
  Pure modules use `node:crypto` only for hashing (not in the search pattern).

## Blocked / Questions for the Orchestrator

None.

## Recommended Next Step

Draft/dispatch the blind Grok judge handoff (H022) that consumes
`agents/gate2/H022_payloads.json`, writes raw `review.gate2` into scenario JSON,
then a small follow-up to turn on fail-closed missing-result enforcement for
reviewed/active.
