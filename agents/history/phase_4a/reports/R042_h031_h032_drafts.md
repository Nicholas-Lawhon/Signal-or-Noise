# R042 - Orchestrator Draft for H031 and H032

**Role:** Orchestrator
**Handoffs:** H031, H032
**Date:** 2026-07-09
**Status:** approved

## Summary

Drafted two handoffs, both routed to GPT 5.6 Terra (High) with user approval,
designed to run **concurrently**:

- `agents/handoffs/H031_doc09_generation_readiness_review.md` — Consultant
  memo C004 assessing doc 09's generation readiness for Part B against the
  H028→H030 gate evidence: fact-bank workflow, Hard-first ordering, red-team
  likely-guess lists, generation prompt template, plausible-count/threshold
  calibration, D031 abstractness option space, and a D034-sized (40-card)
  batch workflow. Memo-only deliverable with a numbered Decision Points list.
- `agents/handoffs/H032_netflix_identity_rewrite.md` — targeted Netflix
  Medium+Hard rewrite (fourth pass, one seed) focused on decoupling the prose
  frame from the boom-collapse lookback silhouette the judge keys on. Frozen
  fields and Easy evidence protected as in H029; explicit stop-and-escalate
  rule instead of going vague; fallback if the rejudge fails again is seed
  replacement (user call).

Concurrency design: write scopes are disjoint. H032 owns
`scenario_netflix_2012_2017.json`, `agents/gate2/H032_payloads.json`, and
`progress.md`; H031 writes only the C004 memo and its own handoff status, is
barred from `progress.md` (orchestrator logs it at review), from the Netflix
seed, and from running pnpm commands while content may be mutating.

## Decision recorded alongside

D034 (user approved after playtesting): Part B MVP card target cut from 100
to 40 (mix 24/12/4). Synced `roadmap.md`, `docs/04_mvp_scope.md`,
`docs/09_content_and_round_creation.md`, `docs/10_agentic_coding_handoff.md`.
Historical artifacts left unchanged. User also confirmed via Gate 1
playtesting that the abstract Hard cards leak no company information; D031
threshold refinement stays open (input expected from C004).

## Verification

Not run - decision/handoff/report drafting only.

## Next Step

User dispatches H031 and H032 (may run simultaneously). H031 completes with
memo `agents/consultations/C004_doc09_generation_readiness.md`; H032
completes with `agents/reports/R044_H032.md`. Orchestrator reviews both, then
drafts the two-payload blind Netflix rejudge (H033).
