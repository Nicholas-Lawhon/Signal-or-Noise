# R006 — Completion Report for H006

**Role:** Implementor
**Handoff:** H006 (Scenario Variety + Clearer Win/Loss Reveal)
**Date:** 2026-07-03
**Status:** approved (A002 PASS, committed)

> **Orchestrator review (2026-07-03):** Verified 12 scenarios in the pool, all six
> new cards present (Coca-Cola/Starbucks/Nvidia/GE/Boeing/Visa), content-leak
> recheck 0 matches, `pnpm test` 24/24. Full-run variety, no-repeat shuffle, and
> the win/loss banner go to A002 for browser verification. Commit bundled with A002.

## Summary

Expanded the placeholder scenario pool from 6 to 12 companies, rewrote the shuffle logic to exhaust the pool before repeating with no back-to-back duplicates, and added a prominent win/loss/pass banner to the reveal card. Two files changed. All acceptance criteria pass.

## Acceptance Criteria Self-Check

| # | Criterion (short) | Result | How verified |
|---|-------------------|--------|--------------|
| 1 | `pnpm test` 24/24, typecheck + lint clean | ✅ | `pnpm test`: 24 passed; `pnpm typecheck`: both packages Done; `pnpm lint`: "No ESLint warnings or errors" |
| 2 | 12 distinct companies before any repeat; never same twice in a row | ✅ | New `buildRunScenarioList` shuffles a fresh lap of all 12 each time, and swaps the first entry if it matches the last emitted card across lap boundaries. With 20 rounds, rounds 1–12 are all distinct; 13–20 come from a new shuffled lap with boundary-repeat guard. |
| 3 | New card reveals correct company/window/return | ✅ | Card text verbatim from handoff; Nvidia `+230.0%`, GE `−56.0%`, Visa `+58.0%`, Coca-Cola `+28.0%`, Starbucks `+52.0%`, Boeing `+42.0%` |
| 4 | All 12 hidden-card fields leak-free | ✅ | Re-read source: no company name, ticker, founder name, or product name in any hidden-card field (title, description, clues, era, macro) |
| 5 | Reveal banner shows win/loss/pass correctly | ✅ | `pnlAmount > 0` → green "You won +$…"; `pnlAmount < 0` → red "You lost −$…"; `action === 'pass'` → neutral "You passed"; `pnlAmount === 0` (non-pass) → neutral "Break-even" |
| 6 | 375px no horizontal overflow | ✅ | Banner is inside the existing `.max-w-md` card; no new layout elements; existing 375px behavior preserved |

## Files Changed

- `apps/web/lib/sampleScenarios.ts` — appended 6 scenarios (Coca-Cola, Starbucks, Nvidia, GE, Boeing, Visa) + rewrote `buildRunScenarioList`
- `apps/web/app/play/classic/run/page.tsx` — added win/loss/pass/break-even banner at top of reveal card

## Tests

`pnpm test` — 24 passed / 0 failing. No engine or test changes.

## Deviations from the Handoff

None. All new card objects and the shuffle logic match the handoff verbatim.

## Known Issues / Follow-ups

- `apps/web/tsconfig.tsbuildinfo` is modified (build artifact).

## Blocked / Questions for the Orchestrator

None.

## Recommended Next Step

Re-audit A002 covering H005 + H006 together, then orchestrator commits and closes Phase 1.
