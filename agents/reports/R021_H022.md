# R021 - Completion Report for H022

**Role:** Implementor  
**Handoff:** H022  
**Date:** 2026-07-09  
**Status:** approved

## Summary

Fixed H021’s browser-safety regression and `gate2 check` load path. Replaced
Node crypto hashing with a pure in-repo SHA-256 so
`validation → evaluate → payload` stays web-safe through the package root.
`gate2 check` now loads with `skipGate2` and reports stale/wrong-pin/threshold
issues as structured findings. H021 public behavior (optional Gate 2, missing
does not fail validate) is preserved. `pnpm build` passes.

## Acceptance Criteria Self-Check

| # | Criterion (short) | Result | How verified |
|---|-------------------|--------|--------------|
| 1 | `pnpm build` passes | pass | Next.js production build compiled |
| 2 | content tests pass | pass | 50 passed |
| 3 | root `pnpm test` | pass | 87 (37 engine + 50 content) |
| 4 | `pnpm typecheck` | pass | content + engine + web |
| 5 | content validate | pass | 6/6 |
| 6 | gate2 export 18 payloads | pass | `agents/gate2/H022_payloads.json` |
| 7 | gate2 check exit 0, missing=info | pass | 0 errors, 18 missing |
| 8 | bad stored result → structured errors | pass | unit: `skipGate2` + evaluate findings |
| 9 | no API keys; no crypto in browser path | pass | grep: only `run.ts` fs/path/url |

## Files Changed

**Created**
- `packages/content/src/gate2/sha256.ts` — pure SHA-256
- `agents/reports/R021_H022.md`

**Modified**
- `packages/content/src/gate2/payload.ts` — browser-safe hash
- `packages/content/src/gate2/run.ts` — check loads with `skipGate2: true`
- `packages/content/src/validation.ts` — `ValidateScenarioOptions.skipGate2`
- `packages/content/tests/gate2-payload.test.ts` — known vectors + pure path
- `packages/content/tests/validation.test.ts` — skipGate2 structured-error proof
- `agents/gate2/H022_payloads.json` — re-exported
- `agents/handoffs/H022_h021_browser_safety_fix.md` — status complete
- `progress.md`

## Tests

```text
pnpm build                                              → pass
pnpm --filter @signal-or-noise/content test             → 50 passed
pnpm test                                               → 87 passed
pnpm typecheck                                          → pass
pnpm --filter @signal-or-noise/content validate         → 6/6
gate2 export                                            → 18 entries
gate2 check                                             → 0 errors / 18 missing info
sha256Hex("abc") === ba7816bf…0015ad                    → pass
```

## Deviations from the Handoff

None.

## Known Issues / Follow-ups

- Scenario ids in export still embed company slugs (pre-existing H021 note).
- Missing Gate 2 still does not fail validation (intentional until judge writes).

## Blocked / Questions for the Orchestrator

None.

## Recommended Next Step

Orchestrator reviews R021 + full uncommitted H021+H022 diff; commit on
approval; then draft H023 blind Grok judge handoff.
