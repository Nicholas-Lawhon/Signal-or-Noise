# packages/content

Scenario content pipeline — schemas, seed JSON, and validation scripts arrive in Phase 3. Prototype sample data lives in `apps/web/lib/sampleScenarios.ts` until then (decision D006).
